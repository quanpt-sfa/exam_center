# Exam Center Agent Instructions

- This repository is the standalone current-runtime workspace.
- Use only repo-relative paths.
- Read `manifest.yaml` before opening subtrees.
- Open at most 3 files before touching code.
- Prefer local `AGENTS.md` inside `backend/`, `frontend/`, `worker/`, `database/`, and `contracts/`.
- Use `QUICK_EDIT.md` for the shortest edit path.
- Do not scan the full repository.
- Do not use historical files unless a human explicitly requests them.
- Treat `backend/`, `frontend/`, `worker/`, `database/postgres/`, `contracts/`, and `docs/` as the active roots.
