# apps-next Agent Instructions

- This is a staged current-runtime workspace.
- Use only repo-relative paths.
- Read `manifest.yaml` before opening subtrees.
- Open at most 3 files before touching code.
- Prefer local `AGENTS.md` inside `backend/`, `frontend/`, `worker/`, `database/`, and `contracts/`.
- Do not scan the full repository.
- Do not use historical files unless a human explicitly requests historical audit.
- Do not edit existing runtime from this task.
