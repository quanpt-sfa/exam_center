# Contracts Staging Notes

- Contract copy phase has been staged from `contracts/api/**`, `contracts/deployment/**`, and `contracts/worker/**`.
- Read `manifest.yaml` and this file before opening contract areas.
- Do not copy additional contract files unless they are listed in inventory and pass provenance checks.
- Keep contract work within `contracts/**`.
