# Worker Staging Notes

- Worker copy phase has been staged from `worker/**`. Do not copy additional worker files unless they are listed in inventory and pass provenance checks.
- Read `manifest.yaml` and this file before opening worker files.
- Keep worker work within `worker/**`.
