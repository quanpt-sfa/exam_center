[pytest]
markers =
    postgres: PostgreSQL-backed worker tests that require a live database target.
    integration: Worker integration tests that require explicit external runtime dependencies.
    e2e: End-to-end worker flows that span multiple runtime components.
