# Database Staging Notes

- PostgreSQL copy phase has been staged from `database/postgres/**`.
- Read `manifest.yaml` and this file before opening database files.
- Do not copy additional database files unless they are listed in inventory and pass provenance checks.
- Keep database work within `database/**`.
