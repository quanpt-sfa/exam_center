# Backend Staging Notes

- Backend copy phase has been staged from `backend/**`.
- Read `manifest.yaml` and this file before opening backend files.
- Do not copy additional backend files unless they are listed in inventory and pass provenance checks.
- Keep backend work within `backend/**`.
